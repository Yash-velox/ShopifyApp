# Environment profile: Image-Enhancement-UAT
# Fill client_id with this Partner app's Client ID, then run: bash scripts/switch-app-env.sh
# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration
#
# Tunneling (Retention Hub shape) — current local session:
# - Frontend (React UI)  → Cloudflare  → application_url
# - Backend (FastAPI)    → Cloudflare   → app_proxy.url + products create/update webhooks
#
# Product webhooks: prefer relative /webhooks/products/* on the app host (Node HMAC → FastAPI).
# Local quick-tunnel override may point products/update at absolute Backend URL.
# Quick tunnels change every restart — `dev-all.sh` refreshes URLs via update-tunnel-urls.py + deploy.
# With automatically_update_urls_on_dev = false, `shopify app dev` will NOT overwrite them.

client_id = "65b3b854ab6b0345b1848f08e2b92149"
name = "Image-Enhancement-UAT"
application_url = "https://overcome-witch-presented-title.trycloudflare.com"
embedded = true

[access_scopes]
# Start with products; expand for files/images when wiring SOW features
scopes = "write_products,read_products,write_files,read_files,write_metaobjects,write_metaobject_definitions"

[product.metafields.app.demo_info]
type = "single_line_text_field"
name = "Demo Source Info"
description = "Tracks products created by the Shopify app template for development"

  [product.metafields.app.demo_info.access]
  admin = "merchant_read_write"

[metaobjects.app.example]
name = "Example"
description = "An example metaobject definition created by this template"

  [metaobjects.app.example.access]
  admin = "merchant_read_write"

[metaobjects.app.example.fields.title]
name = "Title"
type = "single_line_text_field"
required = true

[metaobjects.app.example.fields.description]
name = "Description"
type = "multi_line_text_field"

[webhooks]
api_version = "2026-10"

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/uninstalled"
  topics = [ "app/uninstalled" ]

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/scopes_update"
  topics = [ "app/scopes_update" ]

  [[webhooks.subscriptions]]
  uri = "https://social-unwrap-hours-paso.trycloudflare.com/internal/webhooks/products-update"
  topics = [ "products/update" ]

  # Same FastAPI intake as update; topic comes from X-Shopify-Topic / body.
  [[webhooks.subscriptions]]
  uri = "https://social-unwrap-hours-paso.trycloudflare.com/internal/webhooks/products-update"
  topics = [ "products/create" ]

[auth]
redirect_urls = [
  "https://overcome-witch-presented-title.trycloudflare.com/auth/callback",
  "https://overcome-witch-presented-title.trycloudflare.com/auth/shopify/callback",
  "https://overcome-witch-presented-title.trycloudflare.com/api/auth/callback"
]

# App proxy → FastAPI backend (Cloudflare quick tunnel)
[app_proxy]
url = "https://social-unwrap-hours-paso.trycloudflare.com"
subpath = "image-enhancement"
prefix = "apps"

[build]
automatically_update_urls_on_dev = false
